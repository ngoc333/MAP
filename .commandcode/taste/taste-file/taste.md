# Taste File
- Communicates in Vietnamese. Confidence: 0.9
- Prefers Blazor WebAssembly over Blazor Server for web applications. Confidence: 0.7
- Prefers Radzen Blazor as the UI component library. Confidence: 0.7
- Prefers TailwindCSS for layout and styling (over Bootstrap or plain CSS). Confidence: 0.7
- Groups Blazor page modules (individual feature pages as Razor Class Libraries) under a `Pages/` folder, while keeping infrastructure projects like Host and Shared at solution root. Confidence: 0.8
- When restructuring project folder layouts, expects ALL references to be updated — not just the solution file (`.slnx`) but also `ProjectReference` paths inside `.csproj` files. Partial updates that leave broken references are not acceptable. Confidence: 0.7
- Prefers dynamic/lazy-loaded module architecture using Razor Class Libraries and `LazyAssemblyLoader` — each page lives in its own DLL, downloaded on first access and cached thereafter. Confidence: 0.6
- Prefers JSON-based configuration for menus/navigation (e.g., `page.json`). Confidence: 0.5
- Targets the latest available .NET version (currently .NET 10). Confidence: 0.6
- In .NET solution files (`.slnx`), prefers projects directly under `<Solution>`, avoiding arbitrary nesting like `/src/` or `/src/Modules/`. However, will use logical grouping paths (e.g., `Pages/`) for related module projects when it adds clarity. Confidence: 0.85
- Prefers a mostly flat physical folder structure — avoids `src/` or other intermediary nesting. However, groups related page/module projects under a logical folder like `Pages/` while keeping infrastructure projects (Host, Shared) at solution root. Confidence: 0.8
